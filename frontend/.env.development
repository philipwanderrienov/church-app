# This file defines environment variables for the frontend in development mode.
# The VITE_ prefix is required by Vite to expose the variable to the client-side code.
VITE_API_BASE_URL=http://localhost:8080/api/v1